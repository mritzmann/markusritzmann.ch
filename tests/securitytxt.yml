# https://securitytxt.org
securitytxt:
  stage: test
  image: debian:latest
  variables:
    GIT_STRATEGY: none
  script:
  - apt update
  - apt -y install wget
  - wget https://${FQDN}/security.txt
  - wget https://${FQDN}/.well-known/security.txt
